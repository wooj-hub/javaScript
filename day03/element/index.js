// //기본: str,number,null,undefined,boolean
// //참조: array, element

// const newDiv = document.createElement("div");
// const h1 = document.createElement("h1");
// h1.innerText="토요일";
// newDiv.appendChild(h1);

// const newDiv = document.querySelector(".box");
// //여러개의 class 명을 list로 출력함
// console.log(newDiv.classList);

// newDiv.classList.forEach((v) => {
//   console.log(v);
// });

// newDiv.classList.add("pretty");
// newDiv.classList.remove("large");
// newDiv.classList.contains("box");
// newDiv.classList.replace("box", "wrapper");
// //toggle: 있으면 빼고 없으면 넣어줘
// newDiv.classList.toggle("large");

// newDiv.classList.forEach((v) => {
//   console.log(v);
// });
